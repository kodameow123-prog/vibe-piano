{
  "version": 2,
  "builds": [
    { "src": "index.html", "use": "@vercel/static" },
    { "src": "midi/**", "use": "@vercel/static" }
  ],
  "routes": [
    { "src": "/midi/(.*)", "dest": "/midi/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}