{
  "version": 2,
  "builds": [
    { "src": "index.html", "use": "@vercel/static" }
  ],
  "routes": [
    { "src": "/midi/(.*)", "dest": "/public/midi/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}